# KeyCliq – Variables de entorno (copiar a .env o configurar en Vercel)
# Documentación: docs_knowledge_transfer/ENVIRONMENT_SETUP.md

# Base de datos (obligatorio)
# En Vercel: usar Vercel Postgres, Neon, o tu Postgres remoto
DATABASE_URL=postgresql://user:password@host:5432/database

# Sesión (obligatorio) – generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-long-random-secret

# OpenAI – necesario para escaneo de llaves
OPENAI_API_KEY=sk-...

# URL pública de la app (para emails y redirects)
APP_URL=https://tu-dominio.vercel.app

# Email (producción)
MAIL_FROM_NAME=KeyCliq
MAIL_FROM_ADDRESS=no-reply@tudominio.com
RESEND_API_KEY=re_...

# Cloudinary (opcional; si no se configura, imágenes solo en sesión)
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=
# CLOUDINARY_UPLOAD_PRESET=

# Entorno
NODE_ENV=production
